// this function blacks out everything that is larger or outside the 16:9 rectangle. it is currently not used because nothing actually does go over that edge so backGround(0); does the trick
/*
//draws balck boxes to make the screen 16:9
public void drawScreenBorders() {
  noStroke();
  fill(0);
  if(blackW == 0) {
    rect(0, -blackSize, bWidth, blackSize);
    rect(0, bHeight, bWidth, blackSize);
  }
  else {
    rect(-blackSize, 0, blackSize, bHeight);
    rect(bWidth, 0, blackSize, bHeight);
  }
}
*/
